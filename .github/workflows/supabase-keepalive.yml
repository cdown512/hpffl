name: Supabase keep-alive

# Supabase pauses Free Plan projects after ~7 days of low activity. This pings
# one table twice a week so the inactivity timer never runs out.
#
# Setup: repo → Settings → Secrets and variables → Actions → New repository secret
#   SUPABASE_URL   https://wsvlicolbjwppnamuziw.supabase.co
#   SUPABASE_KEY   your sb_publishable_... key
# (The publishable key is enough — this only reads.)

on:
  schedule:
    - cron: '17 13 * * 1,4'   # Mondays and Thursdays, 13:17 UTC
  workflow_dispatch:           # also runnable by hand from the Actions tab

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the database
        run: |
          code=$(curl -s -o /tmp/out.txt -w '%{http_code}' \
            "https://wsvlicolbjwppnamuziw.supabase.co/rest/v1/franchises?select=id&limit=1" \
            -H "apikey: sb_publishable_Cf9vGH0Xe04pmN2Z9uXe3A_HhqvwlcR")
          echo "HTTP $code"
          cat /tmp/out.txt
          if [ "$code" != "200" ]; then
            echo "::error::Supabase ping failed with HTTP $code — the project may be paused."
            exit 1
          fi
